#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @ch: int to be checked
 * Return: 1 if ch is a digit, 0 otherwise
 */
int _isdigit(int ch)
{
	return (ch >= '0' && ch <= '9');
}
